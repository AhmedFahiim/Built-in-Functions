function stringRepeat(number) {
  let string = "";
  for (let i = 0; i < number; i++) {
    string += this;
  }
  return string;
}

String.prototype.stringRepeatfun = stringRepeat;

console.log("ahmed".stringRepeatfun(2));
